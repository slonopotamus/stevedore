---
description: Makes a new release
---

## Instructions

Update version in `Cargo.toml`.

Update version in `CHANGELOG.adoc`.

Create a named tag without `v` in name with changelog (see how previous tags are made).
